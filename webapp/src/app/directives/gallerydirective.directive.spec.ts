import { GallerydirectiveDirective } from './gallerydirective.directive';

describe('GallerydirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new GallerydirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
